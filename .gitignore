# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# 학생/학부모 개인정보 엑셀·CSV (git에 올리지 않음)
*.xlsx
*.xls
*.csv
# 학부모 계정 발급 결과 (아이디·평문 비밀번호 — 절대 커밋 금지)
학부모 계정 발급.csv
*계정 발급*.csv

# pdf.js 워커 (빌드 시 node_modules 에서 자동 복사)
/public/pdf.worker.min.mjs

# 시험지/해설지 원본 (앱에서 업로드해 쓰므로 저장소에는 넣지 않음)
*.pdf

/.next-test/

/.next-build/
/.next-dev/
